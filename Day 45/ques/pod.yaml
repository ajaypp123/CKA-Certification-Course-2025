apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx-pod
  name: nginx-pod
spec:
  imagePullSecrets:
  - name: dockerhub-secrets
  containers:
  - image: docker.io/ajaypp123/test-private-repo:0.0.1
    name: nginx-pod
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
